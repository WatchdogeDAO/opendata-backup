const fs = require('fs');

fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
fs.writeFileSync('nene.json', JSON.stringify({nene: 1}));
